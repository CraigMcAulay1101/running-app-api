service: spotipy-test
plugins:
  - serverless-offline
provider:
  name: aws
  runtime: python3.7
functions:
  getURL:
    handler: handlers/auth.getauthurl
    events:
      - http:
          path: /authurl
          method: get
  getaccesstoken:
    handler: handlers/access.getaccesstoken
    events:
      - http:
          path: /access
          method: post
          cors: true